# 绕线助手 26.3.5

这是一个针对 `.bin` 存档导入体验的小修复版本。

## 修复

- `.bin` 存档导入确认按钮改为“导入并打开”。
- 导入完成后会立即载入对应项目和保存进度，无需再进入项目管理手动打开。
- 同名存档的保留与覆盖分支也会在操作完成后打开项目。
- “微信小程序版”菜单现在可直接查看小程序码、复制完整入口口令。

`.sar` 存档仍保持原来的“仅导入到项目管理”行为。
